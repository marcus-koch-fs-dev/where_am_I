import dotenv from 'dotenv'
// import errorReporter from './errorReporter'

export default function Client() {
    dotenv.config()
    // errorReporter.client.report(new Error('Could not log in the account'))
    return
}